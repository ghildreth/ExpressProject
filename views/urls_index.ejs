<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
  <% include ./partials/_header %>
</head>
<body>
  <h2> List URLs: Shorted to ID --- full URL </h2>

  <ul>
    <% for (let key in urls) { %>
      <li><%= key %> --- <%= urls[key] %> </li>
      <form method="POST" action="/urls/<%= key%>/delete">
      <button type="submit">Delete</button>
    <% } %>
      </form>
  </ul>
  <% username%>
    <a href = "/">Link for Shorted URLs</a>

</body>
</html>